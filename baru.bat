@echo off
color a 

echo INSTALLING...

pip install -q requests
pip install -q selenium
pip install -q undetected-chromedriver
pip install -q pystyle
pip install discord.p
pip install time 
pip install calendar 
pip install traceback
pip install threading 
pip install pysocks
pip install random
pip install json
pip install tracemalloc 
pip install asyncio 

echo
echo

pause
